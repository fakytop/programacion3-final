@model IEnumerable<LogicaNegocio.Entidades.Country>

@{
    ViewData["Title"] = "Países";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create new</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.Id)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Name.Value)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.IsoAlfa3.Value)
            </th>
            <th>
                @Html.DisplayNameFor(model => model.Region.Value)
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.Id)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Name.Value)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.IsoAlfa3.Value)
            </td>
            <td>
                @Html.DisplayFor(modelItem => item.Region.Value)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Details", "Details", new { /* id=item.PrimaryKey */ }) |
                @Html.ActionLink("Delete", "Delete", new {id=item.Id})
            </td>
        </tr>
}
    </tbody>
</table>
